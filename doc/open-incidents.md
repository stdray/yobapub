# Открытые инциденты

Список текущих инцидентов, ждущих проверки на реальном устройстве, или требующих доп. данных. После закрытия — переносим в `decision-log.md` со статусом и удаляем отсюда.

Когда юзер начинает новую сессию или жалуется на воспроизведение, ассистент должен сверяться с этим списком и напоминать про открытые пункты.

---

## 2026-06-11 — чёрные поля по бокам на Tizen 2.3 (анаморфный поток) — ПЕРЕОТКРЫТ

**Контекст:** Tizen 2.3, поток `527767.m3u8` (coded 960x704, SAR 4:3, DAR 20:11). Декодер/плоскость 2.3 игнорирует SAR → пиллабокс ~4:3. На Tizen 3.0 (modern hls.js) всё ок.

**Что сделано (хронология попыток, decision-log 2026-06-11):**
1. CSS `transform: scale()` через AspectFixer — **не работает**: плоскость рендера игнорирует трансформы (TraceId c355).
2. tkhd display width (патч hls.js legacy) — **не работает**: `videoWidth` стал 1280 (WebKit взял), но поля остались. Урок: `aspect none` в логе ≠ успех на экране.
3. avc1/stsd display width + pasp 1:1 — **не работает** (2.1.1-20, сессия 22:54Z: играет, vw=1280, поля на месте).

**Вывод:** плоскость рендера берёт размеры из SPS декодера. Контейнер и CSS исчерпаны. Осталось: (а) нативный HLS платформы (`video.src=m3u8`) для легаси-режима — DVB-пайплайн уважает анаморф, но перестройка плеера; (б) транскод на проксе — дорого; (в) принять как косметику. Нужно решение юзера — отдельная задача `anamorphic-aspect-tizen23` на доске work.

**Загадка (низкий приоритет):** на 2.1.1-18 parsePaspBox в одних сессиях давал 4:3 (8a4f), в других 1:1 (15b0) на одном потоке — не объяснено, после нейтрализации pasp неактуально.

**Напоминать:** при обсуждении дальнейшей судьбы фикса; при жалобах на геометрию на 2.3.

---

## 2026-04-26 — пропадания звука / рассыпание картинки на Tizen 2.3 + 720p (legacy hls.js)

**Контекст:** Tizen 2.3, "Старый телевизор" вкл (= legacy hls.js 0.14), 720p. Юзер: "бесконечные пропадания звука, рассыпание картинки, зависания, иногда воспроизведение не могло продолжиться даже после паузы". Регрессия "сравнительно недавно".

**Что сделано:**
- Зависание после fatal `bufferStalledError` починено (decision-log 2026-04-26 08:15, см. ниже). Причина: `recoverMediaError()` сбрасывал `ct` в 0. Заменено на `stopLoad+startLoad(ct)`. Ждём подтверждения на устройстве.

**Что не диагностировано:**
- Пропадания звука и рассыпание картинки. В логе `cq-bu7uYgkaE0MIHto3Q2A` они не видны — лог обрезан до ~100 строк, ранние FRAG_LOADED/LEVEL_SWITCHED/audio-events вытеснены потоком KEY_RIGHT. Возможные причины:
  - Декодер не тянет битрейт (хотя 720p должен быть лёгким для Tizen 2.3)
  - Сеть/прокси режет полосу — фрагменты долетают рваными
  - ABR держит уровень выше реальной полосы
  - Какая-то регрессия в FRAG-pipeline (искать в коммитах за последний месяц)

**Новые данные 2026-06-11:** воспроизведение пропадания звука с ПОЛНЫМ логом — `tmp/sound-loss-20260611-trace-15b0.tsv` (trace 15b0, 2.1.1-18, Льюис S8E2, 960x704). Картина: resume на 433 → 90с играет → перемотка в начало (seek 0) → 7.7 минут ровного воспроизведения БЕЗ единого warning/stall — но звук, по словам юзера, пропал. Маркеров в логе НЕТ. Единственная аудио-аномалия: `AAC PES did not start with ADTS header,offset:14` на sn=1 — повторяется каждую сессию (легаси-старт всегда трогает sn=1), hls.js ресинкается. Кандидаты: сдвиг audio PTS при ремуксе после ADTS-ресинка; аудиодекодер глохнет после backward seek через две буферные зоны.

**Что нужно от юзера для следующего шага:**
- КОГДА пропал звук: сразу после перемотки в начало или позже посреди воспроизведения? Был ли звук на отрезке 433→523 (до перемотки)?
- Помогает ли пауза/play; помогает ли seek вперёд-назад
- Воспроизводится ли на неанаморфном тайтле (другой сериал/фильм)

**Напоминать:** при любом новом отчёте о проблемах воспроизведения на Tizen 2.3, и при следующей сессии.

---

## Шаблон записи

```
## YYYY-MM-DD — краткое описание инцидента

**Контекст:** платформа, версия, симптомы со слов юзера

**Что сделано:** ссылки на коммиты / decision-log entries

**Что не диагностировано:** перечисление неподтверждённых гипотез

**Что нужно от юзера для следующего шага:** конкретный список

**Напоминать:** триггеры — когда поднимать тему
```
