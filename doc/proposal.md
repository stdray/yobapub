# Предложения по улучшению (Android APK)

| # | Что сделать | Влияние (1-5) | Сложность (1-5) | Заметки |
|---|-------------|:---:|:---:|---------|
| 1 | Убрать `addOnLayoutChangeListener` — вычислять scale один раз при `onCreate` через `DisplayMetrics` | 2 | 1 | Сейчас scale пересчитывается при каждом изменении layout, включая рендер видео |
| 2 | Предзагрузка медиа-ссылок (`/v1/items/media-links`) параллельно с `getItem` | 4 | 2 | Сейчас два последовательных API-запроса до старта видео |
| 3 | Кеширование `media-links` в `sessionStorage` | 3 | 2 | При переключении серий повторно запрашиваются те же ссылки |
| 4 | Убрать лишний `fetchRewrittenHls` когда `selectedAudio === 0` | 3 | 1 | Уже не вызывается — проверить что это так для всех случаев |
| 5 | Перейти с `hls.js` на нативный HLS через `<video src=".m3u8">` на Android (MSE поддерживается в WebView >= Android 5) | 5 | 3 | Убирает JS-парсинг манифеста и сегментов, браузер делает это нативно |
| 6 | Добавить `preload="auto"` или `preload="metadata"` к `<video>` элементу при создании | 2 | 1 | Может ускорить буферизацию первых сегментов |
| 7 | Использовать `http` стриминг вместо `hls` по умолчанию на Android (прямой mp4/mkv) | 4 | 2 | HLS нужен только для смены аудио; для дефолтной дорожки прямой URL быстрее |
| 8 | Overlay с иконкой Play при старте автовоспроизведения | 1 | 2 | Системный Android overlay при `setMediaPlaybackRequiresUserGesture(false)`, убирается ~1 сек сам |
| 9 | Офлайн-страница при недоступности сервера | 2 | 2 | Показывать внятное сообщение вместо ERR_NAME_NOT_RESOLVED |
| 10 | Back из плеера не работал — передавать `keydown(8)` в WebView вместо прямого `appGoBack()` | 5 | 2 | Исправлено в текущей сессии |
